generates:
  index.d.ts:
    schema: src/data/schema.graphql
    plugins:
      - typescript
    config:
      maybeValue: T
      skipTypename: true
      avoidOptionals:
        field: true
        object: false
        inputValue: false
